doctype xml
document
  head
    :style
      .ordinalLayout {
        tv-content-align: center;
      }
      .whiteButton {
        tv-tint-color: rgb(255, 255, 255);
      }
      
  compilationTemplate(theme="dark")
    list
      relatedContent
        itemBanner
          heroImg(src="#{baseUrl}/resources/images/italy/italy_20_square.jpg")
          row
            each action in ['button-add-alt', 'button-rate', 'button-more']
              buttonLockup
                badge.whiteButton(src="resource://#{action}")
                title Title 
      header
        title Title
        subtitle Subtitle
        row
          text Text 1
          text Text 2  
          
      section
        header: title Title
        each val in [1, 2, 3, 4, 5, 6, 7, 8, 9]
          listItemLockup
            ordinal.ordinalLayout(minLength="2")= val
            title Title #{val}
            subtitle Subtitle #{val}
            decorationLabel Right label #{val}
